import { userReducer, userActions } from "./model/slice/user.slice";
import type { IUser, IUserSchema} from "./model/types/user.state"

export {
    userActions,
    userReducer,
    IUser,
    IUserSchema
}